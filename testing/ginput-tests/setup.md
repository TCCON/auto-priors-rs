To set up this directory to run the tests that actually execute ginput:

1. Clone ginput into `py-ginput` in this directory, create its Python environment, and ensure the `run_ginput.py` script is created.
2. Download the GEOS files in the various `.txt` lists under `met`, `chm-fpit`, and `chm-it`.
